# Docker Environment Configuration for SpideyPlot
# Copy this to .env.local and customize as needed

# Application Settings
NODE_ENV=production
PORT=3000
HOSTNAME=0.0.0.0
NEXT_TELEMETRY_DISABLED=1

# Data Persistence
DATA_PATH=/app/data

# Performance Settings (for large computations)
NODE_OPTIONS=--max-old-space-size=8192

# Docker Compose Overrides
# Uncomment and modify these as needed:

# Host port mapping (default: 3000)
# PORT=3000

# Data directory on host (default: ./data)
# DATA_DIR=/mnt/user/appdata/spideyplot/data

# Logging
LOG_LEVEL=info

# Future: Database Configuration (when adding backend)
# DATABASE_URL=postgresql://user:password@postgres:5432/spideyplot
# REDIS_URL=redis://redis:6379

# Future: Authentication (when adding user management)
# JWT_SECRET=your-secret-key-here
# AUTH_PROVIDER=local

# Future: External Services
# WEBHOOK_URL=http://unraid-server:8080/api/notifications